# 本轮预设分析规则

在本轮首次外层评估前写定，日期2026-09-25。旧数据已被先前分析，本轮仍属探索性改进。

- 第一问源程序、数据及结果不更改；保留原轻处理和QC。沿用原333个有效Trial、原始顺序五个时间块，四份MAT哈希和剔除规则固定。
- 两形状偏好WC群体的τE=0.06、τI=0.12、延迟0.07、任务增益1固定；连接和源转换沿用原稳定配置。6个基底为即时WC源、0.08/0.25/0.75秒记忆状态、0.3/0.8秒持续状态。共同/差异各一组；全窗0–800ms拟合，不强制晚期归零。
- 记录内两类共用共同/差异系数；同任务记录间收缩。Ridge候选0.1/0.01/0.001/0.0001，记录收缩1/0.1/0；差异Ridge倍数10、收缩倍数2固定。波形选择损失是训练尺度归一化后的留出共同误差与差异误差之和。
- 分类模板固定Ridge=0.01、记录收缩0.1，不使用波形调参结果。差异匹配保留幅值，训练类内残差采用Ledoit–Wolf协方差。
- 分类候选：原4特征、100–500ms的8窗×3通道=24时空均值、上述24维+差异匹配分数；LR的C=0.01/0.1/1及仅24维的自动收缩LDA，总10项。内层BA选择，平手按候选预定顺序。
- 训练拟合、未见Trial稳健ERP预测、未见单Trial预测三个指标分开。原主版历史成绩仅作参照，不用外层成绩挑候选。
- 默认19次完整分类流程置换，保持记录/时间块类别数；所有监督模板、协方差和分类选择重做。后续更多置换只提高分辨率。
- 修改代码或候选后不得覆盖同一结果目录；结果绑定源码、环境和数据指纹。训练拟合更好不等于分类泛化更好，全部结果如实保留。
