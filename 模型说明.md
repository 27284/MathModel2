# 第二问模型说明

本文对应2026-09-25评审修订后的唯一主版本。第一问、原始数据、轻处理及333个有效试次不变。完整文件导读见同目录《第二问实现方法与代码导读.md》。

## 1. 两条路线分别验证

响应路线：图像 → DoG/Gabor → 两个形状偏好驱动 → Wilson–Cowan群体 → 时间基函数 → 三通道共同/差异响应。

分类路线：单试次脑电 → 100–500ms的24维时间窗均值 → 训练内标准化 → 逐记录固定逻辑回归。

主分类不使用机理差异分，也不使用QC样本权重。QC×Huber仍用于条件ERP。机理特征仅作为预设分类对照，不能凭训练拟合好就宣称分类有效。

## 2. 固定神经动力学

```text
0.060 dE/dt = -E + sigmoid(5E - 6I + u - 2)
0.120 dI/dt = -I + sigmoid(5E - I - 2)
u_k(t) = φ_k × 实际提示脉冲(t - 0.070)
0.020 dq_k/dt = -q_k + (E_k-E0) - 0.6(I_k-I0)
```

从静息平衡点开始，每采样点4个RK2子步。时间常数、连接参数和任务增益固定；静息Jacobian仅检查局部稳定性，不反演真实生理参数。

φ来自两幅几何镜像图：零直流DoG、12方向正交Gabor能量、空间方向图余弦匹配和softmax。两个群体表示形状偏好，不代表左右半球。空间图进入动力学前被压为两个权重，当前仍无真实皮层网格或解剖头模型。

## 3. 共同/差异与两个响应候选

```text
M = (ERP左 + ERP右) / 2
D = (ERP右 - ERP左) / 2
预测_r,s(t) = C_rᵀ B_common(t) + s D_rᵀ B_contrast(t)
s=-1为左，s=+1为右
```

共同和差异系数分别拟合；从未施加“差异=共同波形×常数”的硬约束。

每个分量6个基函数。原结构 `legacy` 包括原始源、80/250/750ms记忆状态和300/800ms阶跃持续状态。新候选 `transient_contrast` 的共同基函数不变，差异项后两列改为神经源提前/延后40ms；不直接使用持续阶跃，也不保证彻底排除漂移。

处理顺序为原始源/阶跃 → 因果状态 → 30Hz零相位低通 → 截窗和基线 → 固定模拟参考RMS定标。定标不读取EEG，参考提示持续52/256秒。

共144个名义线性系数。拟合窗口0–800ms，按训练条件有效样本量加权。共同部分的Ridge为λ、同任务收缩为γ，差异部分分别为10λ、2γ。候选λ={0.1,0.01,0.001,0.0001}，γ={1,0.1,0}。主响应在两结构共24组候选中做内层选择，原结构对照在自身12组候选中选择。

## 4. 固定逐记录解码及对照

100–500ms分8个左闭右开的50ms窗，每个通道取均值，共24维。每条记录分别训练StandardScaler和LR(C=0.1, class_weight=balanced)，不做分类超参数搜索。LDA、同任务池化LR、QC加权LR、25维机理差异LR均为预设对照，不按外层表现替换主方案。

机理对照用固定原结构(λ=0.01,γ=0.1)的训练ERP模板，并从训练类内残差估计Ledoit–Wolf协方差Σ：

```text
w = Σ⁻¹d / sqrt(max(dᵀΣ⁻¹d, ε))
匹配分 = (x-m)ᵀw
```

匹配分仅在对照的第25维使用。主模型的四个分类器按记录名保存，未知记录必须先校准。

## 5. 混杂对照与验证

ECG敏感性采用既有1–25Hz心电及±约40/80ms滞后回归；QC敏感性用三通道共12项刺激前指标回归24维特征；顺序敏感性用原始试次序号；另有联合回归与原始刺激前特征负对照。所有回归系数只用训练块估计，诊断不改变主输入。

原始每记录100试次先顺序分五块，再按冻结规则保留83/86/85/79个试次。外层同步留出同号块；分类固定训练，波形在其余四块内再轮换留一块选参。所有测试试次只得到一次外层预测。

在同一留出ERP目标上比较主响应、原结构、删除差异项和直接训练ERP模板；分别报告0–800、0–500、250–500和600–800ms，以及joint/common/contrast三个分量。单试次原幅值评分另报。

默认200次记录/块内标签置换，完整重训主分类器和标准化。p=(1+不低于实际BA的置换数)/(1+置换数)。主分类没有监督模板和选参，因此置换不运行无关波形流程。BA区间为固定OOF预测的记录内时间块bootstrap，不代表全部训练不确定性。

## 6. 本轮实际结果与结论边界

合并BA=55.75%（修订前49.55%）；任务1=60.67%，任务2=50.74%。200次置换探索性p分别为0.0448、0.00995、0.4975，分组p未做多重比较校正。总合并p接近0.05且置换精度有限，不应写成稳定确证。

主响应留出ERP R²=0.2688，原结构=0.2671，直接训练ERP模板=0.2817；250–500ms仍为负R²。全数据训练曲线R²=0.9125，有4条曲线为负，不能以该训练指标替代泛化证据。

本轮属于已查看数据上的探索性修订；不证明55%是准确率上限，不证明识别信号具有特定生理来源。只有三额区通道，dipole_demo仍未接入，不声称真实空间定位；零相位预处理也不支持500ms在线决策声明。

冻结规则见 `references/本轮评审修订规则.md`。未采用约30个平移/伸缩基函数和任意偶极几何接入，原因分别是小样本复杂度和缺少空间依据。
