# 第二问模型说明

## 1. 从图形到神经驱动

左右图形为几何重建。两项归一化高斯之差提取局部对比，12方向正交Gabor能量保留位置×方向信息，和标准轮廓做余弦匹配后softmax得到两种形状偏好φ。图形朝向不等于图形所在半视野，两种偏好也不等于左右半球。

## 2. 稳定神经群与持续状态

沿用原简化Wilson–Cowan群体：

```text
0.060 dE/dt = -E + sigmoid(5E - 6I + u - 2)
0.120 dI/dt = -I + sigmoid(5E - I - 2)
u_k(t) = φ_k × 实际提示脉冲(t - 0.070)
0.020 dq_k/dt = -q_k + (E_k-E0) - 0.6(I_k-I0)
```

从稳定无刺激平衡态出发，二阶中点法每采样点4子步。时间常数与任务增益固定，不继续拟合已经触界且不可稳定辨识的神经参数。取两源的共同项与差异项，各自进入τ=0.08、0.25、0.75秒的因果低通记忆状态；另有提示后τ=0.3、0.8秒的持续状态。持续状态是等效注意/准备活动，不是已确认的海马来源，也不强制在800ms回零。

每组共6个时间基底；按固定52/256秒提示下的RMS定标，随后施加与观测一致的30Hz低通及基线校正。该定标不读取EEG。只有两种固定图像，编码不能单独证明大脑使用相同视觉算法。

## 3. 共同响应与左右差异

同一记录的观测式：

```text
y_r,s(t) = C_r^T B_common(t) + s D_r^T B_difference(t) + ε(t)
s=-1 为左，s=+1 为右
```

共同项由训练ERP左右平均估计，差异项由右减左的一半估计。它们来自同一组源和观测系数，不能分别为左右条件任意设置两套动力学。

按QC有效样本数加权，求带Ridge与同任务记录间收缩的线性观测系数；差异Ridge为共同项10倍，记录收缩为2倍。共4记录×2分量×6基底×3通道=144个名义系数，但有效自由度由正则化显著限制并实际报告。这是高于原模型容量的等效观测模型，不因拟合好就声称真实神经参数可辨识。

在外层训练集内部，再留一个原始时间块，用共同误差与差异误差之和选择正则；每通道尺度仅由训练ERP确定。生成模型在0–800ms拟合。对照包括删除形状差异的共同模型，以及直接训练ERP模板。

## 4. 特征表示与解码

- 幅值4维：保留原先P300候选窗口的幅值/侧化对照。
- 时空24维：100–500ms，8个50ms窗×Fz/F3/F4，不用一个大均值把时间变化抵消。
- 差异25维：上述24维再加一个噪声加权的差异匹配分数。

用训练类内残差估计Ledoit–Wolf收缩协方差Σ。训练生成模型给出24维共同模板m和差异模板d，匹配方向为Σ⁻¹d，再按其噪声方差定标。未知Trial的分数为wᵀ(x−m)。不分别把左右模板缩到单位范数，因此保留模板的幅值差异。记录中心、尺度、Σ、m、d全部来自训练Trial。

为避免分类内层泄漏，分类模板固定Ridge=0.01、记录收缩0.1，独立于波形正则选择。分类器仅在10个预设候选中选：3特征组×3种LR的C（0.01/0.1/1），加24维的收缩LDA。LR使用质量权重与类别平衡；LDA不支持样本权重。外层只评分，不挑外层成绩最高的消融替代主模型。

## 5. 验证和保真边界

原始400Trial中333个满足同样的扩展窗无削顶条件。每记录按原试次序分为五块，各记录同步留出同号块。所有监督步骤在训练内重做，完整分类置换也重做模板、噪声估计和内层选择。

训练拟合是全数据稳健ERP的曲线逼近；留出ERP预测是在未见Trial组上比较稳健响应；单Trial预测则对所有保留测试样本等权评分。R²负值、分类接近机会水平及差通道都如实报告。当前分析复用了已经探索的数据，仍需独立记录确认。

第一问源程序和结果没有改动，本版也不使用第一问最终高斯拟合曲线。低通与QC规则沿用原第二问，未引入新的EMD/ICA/高通或强制正峰。传感器有限，残留眼动、状态和记录变化可能参与响应，不能据此推断疾病。

## 6. 来源

- 用户原《第二问建模方案说明.md》及提供的参考论文用于确定结构主线。
- [Wilson–Cowan原始论文](https://doi.org/10.1016/S0006-3495(72)86068-5)：兴奋/抑制群体思想；当前参数为建模约定。
- [Ledoit–Wolf官方实现](https://scikit-learn.org/stable/modules/generated/sklearn.covariance.LedoitWolf.html)：收缩类内噪声协方差。
- 本轮冻结的候选与评估规则见references/本轮预设分析规则.md。
