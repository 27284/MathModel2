# C题第二问：实现方法与代码导读

> 对照日期：2026-09-25。项目：`D:\pycharm\Math_Model2`。本文对应参考评审报告修订后的唯一主版本，与当前源码和 `outputs/results` 一致。历史方案见 `references`，不代表当前功能。

## 1. 当前程序解决什么问题

第二问要求说明视觉形状怎样引起脑电响应差异，并构造左右三角形的区分特征。当前项目将**响应建模**和**左右分类**分开：

```text
响应：几何三角 → DoG/Gabor → 形状偏好驱动 → 神经群
     → 时间基函数 → 共同/差异线性读出 → 三通道响应

分类：单次脑电 → 100–500ms的24维时间窗均值
     → 训练内标准化 → 逐记录固定逻辑回归 → 左/右及右类概率
```

机理匹配分只用于分类对照，当前主分类器不依赖生成模型。这样可以直接检查：复杂机理是否真的改善预测，而不把漂亮的训练曲线当作识别成功。

**当前还有一个题意缺口：Gabor空间图进入动力学前被压缩为两个权重，尚未实现有解剖依据的皮层空间布局与头皮前向模型。** `dipole_demo()`只是未接入主流程的示意函数，不能说成已完成真实脑源定位。

### 当前结果概览

| 指标 | 修订前 | 当前 | 应如何理解 |
|---|---:|---:|---|
| 全部333试次合并BA | 49.55% | 55.75% | 同记录时间块留出分类，有提升但能力仍有限 |
| 任务1 BA | 50.61% | 60.67% | 本轮主要提升来源 |
| 任务2 BA | 48.42% | 50.74% | 仍接近随机 |
| 留出稳健ERP汇总R² | 0.2671 | 0.2688 | 改善很小，仍低于训练ERP模板0.2817 |
| 全数据训练曲线汇总R² | 0.9382 | 0.9125 | 训练拟合不是预测成绩；当前有4条训练曲线R²为负 |

200次置换的合并探索性p=0.0448，任务1 p=0.00995，任务2 p=0.4975。分组p未做多重比较校正，合并p接近0.05且Monte Carlo精度有限，不应写成稳定确证。数据已被反复查看，本轮是探索性修订，不是新增独立测试集。

### 常用名词

| 名词 | 本项目中的意思 |
|---|---|
| Trial | 一次视觉提示及其附近脑电片段 |
| Record | 一份MAT记录；A/B文件名不等于已确认的独立被试 |
| ERP | 同条件多个试次形成的响应曲线；当前主要采用QC×Huber稳健估计 |
| QC | 用原始刺激前信号标定的质量权重 |
| common / contrast | 左右ERP的均值 / 右减左的一半 |
| OOF | 试次由未使用其所在外层时间块训练的模型给出预测 |
| BA | 左类召回率和右类召回率的平均，与普通准确率分开报告 |

## 2. 文件结构与主入口

以下相对路径均以 `D:\pycharm\Math_Model2` 为根目录。

```text
Math_Model2/
├─ main.py                         唯一完整分析入口
├─ predict.py                      同记录未知试次预测
├─ run_model2.bat                   EEGModel环境双击启动
├─ requirements.txt                六个主要依赖版本
├─ README.md                       快速使用说明
├─ 模型说明.md                     数学模型概要
├─ 第二问实现方法与代码导读.md       本文
├─ eeg_model2/
│  ├─ __init__.py                  包标识
│  ├─ config.py                    固定配置
│  ├─ data.py                      MAT读取、事件、轻处理
│  ├─ preprocess.py                训练内QC和稳健ERP
│  ├─ shape.py                     DoG/Gabor形状编码
│  ├─ neural.py                    Wilson–Cowan群体
│  ├─ response.py                  共同/差异响应和两种基函数结构
│  ├─ features.py                  固定分类与预设对照
│  ├─ diagnostics.py               新增：训练内混杂回归与趋势统计
│  ├─ evaluation.py                外层验证、内层波形选型、评分与置换
│  └─ report.py                    图表和中文结果报告
├─ tests/test_model.py              23项有效检查
├─ data/                           四份原始MAT
├─ references/                     原题、思路、规则、历史对照、审计
├─ outputs/results/                唯一正式结果目录
├─ .run/第二问.run.xml               PyCharm运行配置
├─ .idea/                          PyCharm工程设置
├─ .gitignore                      Git忽略规则
├─ .git/                           版本管理元数据
└─ __pycache__/                    自动运行缓存
```

### main.py的执行顺序

1. 固定配置，读取四条记录，建立形状编码与神经时间基函数库。
2. 计算代码、原始数据、配置、运行库版本签名，拒绝混用不同方案的结果目录。
3. 若核心文件齐全且内容指纹一致则复用；否则执行五折验证与全数据再拟合。
4. 保存分类、波形、混杂、趋势指标，绘制图表。
5. 完成或追加主分类置换，生成最终报告并更新manifest。

默认分析只需要运行 `main.py`。`predict.py`是模型应用入口，不是第二套分析方案；其余模块由主入口调用。

## 3. data.py：从原始记录到试次

### 输入与标签

| 文件 | 原始试次 | 有效试次 |
|---|---:|---:|
| `data/VisualCogA_Task-1.mat` | 100 | 83 |
| `data/VisualCogA_Task-2.mat` | 100 | 86 |
| `data/VisualCogB_Task-1.mat` | 100 | 85 |
| `data/VisualCogB_Task-2.mat` | 100 | 79 |

MAT的 `data` 为10行记录通道，采样率256Hz。程序使用原始1—3通道，顺序固定为Fz、F3、F4；不使用机器处理后的4—6通道。

第8通道VisCue决定事件和标签：`-1`左、`+1`右、`0`无提示。第9通道TgtAct只审计实际应答码，不作为刺激标签；第10通道用于检查时间连续。第7通道ECG用于本轮新增的敏感性对照，不改变主分类输入。

`onsets_and_ends()` 找非零提示平台起止；非零平台直接变号也视为新提示。`read_record()`核对通道、采样率、有限数值和提示码；`load_records()`按A1、A2、B1、B2顺序读取。

### 冻结的轻处理

```text
事件前后原始扩展窗 → 原始饱和/越界检查
→ 四阶30Hz Butterworth零相位低通
→ 截取约-200至+800ms → 减刺激前均值
```

核心采样索引为-51至205，共257点，实际时间为-0.19921875至0.80078125秒；两侧各增加256点，扩展窗为769点。扩展窗内任一原始EEG通道绝对幅值≥999.9或窗口越界就排除试次。当前67个排除试次均为原始饱和。

每条记录先按原始顺序每20试次分为5块，再排除无效试次，不重新分组。程序拒绝扩展窗重叠的输入。第一问去噪结果没有作为这里的输入，原有 `data.py`、`preprocess.py` 和 `config.py` 在本轮均保持原字节不变。

### Record数据结构

| 字段 | 形状/作用 |
|---|---|
| `labels`、`duration`、`onsets` | 各100项：方向、提示持续秒数、原始采样起点 |
| `folds`、`valid`、`reasons` | 原始时间块、有效掩码、剔除原因 |
| `t`、`padded_t`、`core` | 257点核心时间、769点扩展时间、核心索引 |
| `raw_epochs`、`padded` | `100×3×769`的原始/低通扩展窗 |
| `light` | `100×3×257`轻处理脑电，主模型输入 |
| `metrics` | `100×3×4`原始刺激前QC指标 |
| `ecg` | `100×769`既有1–25Hz辅助心电，用于诊断 |
| `prestim` | 原始刺激前4维特征，用于负对照 |
| `background`、`background_ecg` | 保留的辅助背景数组，当前不参与拟合 |
| `audit` | 文件SHA256、单位声明、通道、计数和事件检查 |

无效试次仍占据数组位置，后续使用 `valid` 掩码排除。幅值单位未独立确认为微伏，图中使用“原始文件幅值单位”。

## 4. preprocess.py：QC与稳健ERP

四项QC是基线MAD波动、基线前后半均值差、刺激前最大相邻跳变、较长刺激前区间的线性斜率。均从未做零相位滤波的原始刺激前数据计算。

`quality_weights()`只用训练块估计中位数和稳健尺度，再应用于其他试次。有效权重在0.05—1之间，无效权重为0。`prepare_fold()`返回训练掩码、权重和审计信息。

`huber_erp()`按条件、通道、时间点，以加权中位数初始化，用QC权重×Huber残差权重迭代估计ERP；必要时用二分法求根，形成曲线后再次做基线校正。

**主分类已去掉QC权重，但ERP仍使用原QC×Huber。** 去掉分类权重不等于修改第一问或删除质量控制。

`response.targets()`输出 `4记录×2方向×3通道×257点` 条件ERP，另给有效样本量 `(Σw)²/Σw²` 和所选试次的提示时长中位数。留出ERP也可用相同规则生成，但只用于评分，QC尺度来自训练块。

## 5. shape.py与neural.py：形状驱动神经群

### shape.py中的函数

| 函数/类 | 作用 |
|---|---|
| `triangles()` | 生成65×65左右镜像几何图，不是实际实验屏幕复原 |
| `dog()` | 尺度1与2.8的归一化高斯之差；常量亮度响应为零 |
| `gabor_pair()` | 某方向偶/奇相位滤波器，去均值并L2归一化 |
| `energy_maps()` | 12方向、每15°一个方向的正交相位能量图 |
| `GaborEncoder.encode()` | 展开空间方向图，与左右模板余弦匹配，再做softmax |
| `GaborEncoder.checks()` | 镜像、平移、缺尖端以及图像能量侧化检查 |
| `dipole_demo()` | 假定几何的3电极×2源投影示意，未被主流程调用 |

输出 `phi` 为2种刺激×2种形状偏好群体的权重。图形指向不等于所在半视野，两种偏好不等于左右半球。

### neural.py中的动力学

```text
0.060 dE/dt = -E + sigmoid(5E - 6I + u - 2)
0.120 dI/dt = -I + sigmoid(5E - I - 2)
u = phi × 提示脉冲(t-0.070)，任务增益固定为1
0.020 dq/dt = -q + (E-E0) - 0.6(I-I0)
```

`equilibrium()`求无刺激平衡点；`stability()`检查静息点Jacobian特征值实部。`_simulate()`使用Numba加速RK2积分，一个采样间隔分4个子步。`Simulator.sources()`返回原始E/I/q状态或处理后的源。

原始状态形状为 `试次数×2群体×3状态×769点`。固定时间常数和连接参数是建模约定，本轮没有声称从三通道反演真实神经参数。

## 6. response.py：共同/差异响应与小范围结构调整

### SourceBank如何构造基函数

从参考左图的两源得到共同源 `(q0+q1)/2`、差异源 `(q0-q1)/2`。每个分量均使用6列：

| 列 | `legacy`原结构 | `transient_contrast`受限候选 |
|---|---|---|
| 共同1—4 | 原源、80/250/750ms记忆状态 | 不变 |
| 共同5—6 | 300/800ms阶跃持续状态 | 不变 |
| 差异1—4 | 原源、80/250/750ms记忆状态 | 不变 |
| 差异5—6 | 300/800ms阶跃持续状态 | 差异源提前40ms、延后40ms |

一阶状态递推为 `z[t]=a·z[t-1]+(1-a)·输入[t]`，`a=exp[-1/(fs·tau)]`。持续状态由70ms开始的阶跃驱动，不能说全部基函数都是WC源的平滑副本。

先构造状态，再低通、截窗、基线校正，最后用固定模拟参考RMS归一化。参考提示52/256秒，尺度不使用EEG。`basis(duration, structure)`缓存不同提示时长与结构，输出 `2×6×257`。

受限候选去掉差异项的直接持续阶跃，加入少量潜伏期变化，不等于完全剔除漂移。原结构仍在候选中，内层验证可以选择回原结构。

### fit_response怎样求参数

```text
M = (ERP左 + ERP右)/2
D = (ERP右 - ERP左)/2
m(t) = 共同系数ᵀ × 共同基函数(t)
d(t) = 差异系数ᵀ × 差异基函数(t)
预测左 = m-d，预测右 = m+d
```

共同系数与差异系数分别估计，**没有 `d(t)=k·m(t)` 的硬约束**。每条记录有两分量、六基函数、三通道，四条记录共144个名义系数。

拟合0—800ms训练ERP，正则包括Ridge和同任务A/B记录间的系数收缩。差异Ridge是共同项10倍、收缩2倍。每个分量求解24×24法方程，三个通道共同作为右端项。

候选为两结构×4个Ridge(0.1/0.01/0.001/0.0001)×3个收缩(1/0.1/0)，共24组。`validation_loss()`按内层留出ERP的共同误差与差异误差选参，通道尺度只用内层训练ERP。

`ResponseFit.predict()`生成曲线；`to_dict()`保存结构、正则、系数和条件线性有效自由度。该自由度没有包含ERP估计与调参复杂度。

## 7. features.py：固定逐记录分类

### 主方案

`time_features()`将100—500ms分成8个左闭右开的50ms小窗，每通道取均值，按Fz的8维、F3的8维、F4的8维排列。256Hz下小窗采样数由实际网格决定，不强行按整数13点切分。

每条记录在训练块上独立拟合 `StandardScaler` 和 `LogisticRegression(C=0.1, class_weight='balanced')`。没有分类内层选参，没有QC样本权重；类别平衡仍保留。

`fit_classifier()`保存标准化均值/尺度与分类系数；`classify()`经sigmoid计算右类概率，≥0.5输出+1，否则-1。共保存四个逐记录分类器，而非两个按任务合并的分类器。

### 预设对照PROTOCOLS

| 名称 | 与主方案的差异 |
|---|---|
| `main` | 固定逐记录24维LR |
| `record_lda` | 换成等先验、自动收缩LDA |
| `pooled` | 同任务A/B池化LR；保留训练内逐记录归一化，随后训练池化分类器 |
| `qc_weighted` | LR及其标准化使用原QC权重 |
| `mechanistic` | 加一维机理差异分，固定逐记录LR |
| `ecg_adjusted` | 对训练内ECG回归后的脑电提取24维 |
| `qc_adjusted` | 24维特征减去训练内QC协变量预测偏差 |
| `order_adjusted` | 减去训练内原试次顺序预测偏差 |
| `all_adjusted` | ECG处理后再回归QC和顺序 |
| `prestim_only` | 仅原始刺激前4维特征，负对照 |

十个方案是十个预设对照，**不是十个候选中按成绩挑主模型**。当前主方案固定为 `main`。

### 机理对照如何避免泄漏

`build_features()`仅为 `mechanistic` 构建监督模板：使用固定 `FEATURE_SETTING=(0.01,0.1,'legacy')` 的训练ERP响应，提取模板24维均值m和半差d；从训练类内残差估计Ledoit–Wolf协方差Σ：

```text
w = Σ⁻¹d / sqrt(max(dᵀΣ⁻¹d, epsilon))
差异分 = (x-m)ᵀw
```

匹配分作为第25维。`apply_transform()`供模型应用时复用。主方案不需要这些监督量，所以主分类置换不必重算无关的ERP模板。

## 8. diagnostics.py：训练内混杂敏感性

本模块新增，但不改写 `Record.light` 或有效试次集合。

| 函数 | 实现方法 | 学习边界 |
|---|---|---|
| `ecg_adjust()` | 心电及-20/-10/0/10/20采样滞后项，对三通道脑电做固定岭回归 | 标度、回归系数仅来自训练试次 |
| `residualize()` | 将协变量按训练均值/标准差缩放，回归24维特征，减去预测偏差 | 测试特征和协变量不参与拟合 |
| `diagnostic_features()` | 组合ECG、12项QC、原始试次序号、联合及刺激前负对照 | 每个外层训练块单独估计 |
| `trend_metrics()` | 对全数据条件ERP做描述性线性趋势统计 | 仅描述，不作为留出成绩或来源判据 |

QC协变量为3通道×4项，共12维；试次序号使用原始0—99位置，剔除后不重新编号。`residualize()`的岭惩罚固定为1，ECG回归的平均平方误差尺度惩罚固定为0.01，不按分类成绩调参。

ECG为已有1—25Hz辅助通道，±20点约为±78ms，不是完整的心源去伪影方案。刺激前负对照使用未经零相位滤波的原始基线，避免滤波把刺激后信息带入负对照。

回归后准确率变化不能单独证明或排除生理来源。这些诊断均为探索性，未分别做置换，也不因ECG对照BA更高就事后改为主方案。

## 9. evaluation.py：验证与评分的核心

### 划分与训练

```text
外层测试块0；训练块1/2/3/4
  分类：固定逐记录LR直接训练 → 测试块0
  波形：训练四块内轮换留一块选24组结构/正则
        → 用四块重拟合 → 测试块0
轮换五次，使每个有效试次只被外层测试一次
```

这是同一记录内时间块留出，可能用测试块之后的数据训练，不是仅用过去预测未来；更不是留一被试或新记录验证。

| 方法/函数 | 作用 |
|---|---|
| `Engine.prepare()` | 缓存标签无关的训练QC规则 |
| `Engine.learn()` | 形成训练条件ERP，缓存按块与本轮标签训练的目标 |
| `Engine.select_response()` | 内层选择波形结构及正则；原结构对照从同一内层分数中选原结构最优 |
| `Engine.classify_fold()` | 按固定PROTOCOLS构造记录/池化组，训练并输出预测与完整分类审计 |
| `Engine.run()` | 执行外层验证、对照、诊断、波形计分与文件保存 |
| `Engine.final_model()` | 全数据重新训练四个固定分类器，波形再选参，供展示和以后预测 |
| `score_arrays()`、`erp_scores()` | 统一SSE/SST与窗口、分量口径 |
| `groups()` | 生成all、task1、task2和四条记录的分组 |
| `classification_metrics()` | BA、普通准确率、整块bootstrap区间、混淆计数 |
| `waveform_metrics()` | 单试次等权原幅值评分 |
| `restricted_permutation()` | 同记录同时间块内交换有效标签 |
| `permutation_test()` | 完整重训固定主分类、保存/续跑置换 |
| `write_json()` | 将数组等转为JSON可保存结构 |

全数据模型不用于OOF评分。标签相关的ERP缓存每轮清空；原24维特征和QC不依赖标签，可以复用。置换仍重新训练分类标准化和系数。

### 公平波形对照

| 名称 | 预测来源 |
|---|---|
| `response` | 24组结构/正则经内层选择的主响应 |
| `legacy_response` | 只用原结构12组正则经内层选择 |
| `common_only` | 从主响应删除差异项，没有独立重新拟合 |
| `train_erp` | 直接复用训练条件ERP |

在相同留出目标上比较四者，评分窗为0—800、0—500、250—500、600—800ms。`erp_validation.csv`还区分joint（左右联合）、common（均值）、contrast（半差）。

每组R²为 `1-SSE/SST`，负值保留；汇总R²按总SSE/总SST，不能先平均各组R²。训练单曲线、留出ERP和单试次的SST中心化对象不同，三者不能混作一个成绩。

波形生成需要已知刺激方向作为输入条件；分类和 `predict.py` 不使用未知试次真实方向。这两种“预测”的问题设定不同。

### 置换和条件区间

默认200次，p=(1+置换BA≥实际BA的次数)/(1+置换次数)，最小步长1/201。输出全部七个分组，分组p未经多重比较校正。只置换有效试次，保留每记录每块的左右数量。

置换CSV使用临时文件替换方式保存；完整索引会跳过，残缺索引会重算。增加次数只提高置换精度，不提高准确率。

BA区间来自固定OOF预测的记录内整块bootstrap1000次，不重训分类器，不能称为包含全部模型选择不确定性的区间。

## 10. outputs/results：每个输出文件是什么

| 文件 | 内容及用途 |
|---|---|
| `第二问结果报告.md` | 先留出分类与公平对照，再训练拟合、局限和图 |
| `oof_predictions.csv` | 10方案×333试次，共3330行；真实方向、预测、右类概率、记录、试次与折 |
| `classification_metrics.csv` | 10方案×7分组，共70行；BA、普通准确率、条件区间、LL/LR/RL/RR |
| `confound_metrics.csv` | 主方案与五个混杂/负对照的分组指标子表 |
| `classifier_audit.json` | 外层预设方案、训练试次、标准化、分类系数；替代旧的classifier_selection.json |
| `confound_audit.json` | 每折每记录的ECG/QC/顺序/联合回归系数和训练索引 |
| `training_audit.json` | 原QC训练边界和机理对照匹配参数 |
| `response_parameters.json` | 每外层折24个候选内层损失，主响应与原结构所选参数及系数 |
| `erp_validation.csv` | 折×记录×训练/留出×模型×分量×时间窗的公平ERP评分 |
| `waveform_metrics.csv` | 四记录×四模型×四时间窗的单试次波形评分 |
| `curve_fit_metrics.csv` | 全数据24条训练曲线的R²、RMSE、SSE、SST |
| `trend_metrics.csv` | 全数据条件ERP在0—800、250—500ms的描述性斜率和趋势R² |
| `data_audit.json` | 原始文件SHA256、通道、计数、提示时长、实际应答码等 |
| `shape_checks.csv` | 左右、平移、缺尖端的编码与图像能量检查 |
| `final_model.json` | schema_version=2；四个逐记录分类器、响应模型、记录信息、配置 |
| `manifest.json` | 代码/原始数据/配置/库版本签名、核心结果SHA256、运行状态与置换数 |
| `permutation_scores.csv` | 每次置换×7分组的BA，当前200×7=1400行 |
| `permutation_test.json` | 七组实际BA、置换次数和p值 |
| `run.log` | 分析、复用和置换进度日志，多次运行追加 |

CSV中的 `trial` 从1开始，JSON训练索引从0开始，`fold`从0开始。读取分类表时应同时筛选 `group` 和 `variant`，只筛task可能同时选中任务汇总与记录行。

### NPZ数组文件

| 文件 | 键名与形状 |
|---|---|
| `fitted_curves.npz` | `time_s`；`记录名__target/fitted`为2×3×257，方向轴0左1右 |
| `heldout_erp.npz` | `time_s`；`折__记录名__train或heldout__模型名__0或1__actual或predicted`，每项3×257 |
| `heldout_waveforms.npz` | `time_s`；`记录名__valid/actual/response/legacy_response/common_only/train_erp`，保留原始100试次位置，无效预测为NaN |

本轮 `heldout_erp.npz` 的键名新增模型名，不能继续用旧版键名读取。可用 `numpy.load()` 查看 `.files`。

### 八张图

| 文件 | 含义 |
|---|---|
| `figures/VisualCogA_Task-1_拟合与验证.png` | A任务1训练拟合和留出曲线 |
| `figures/VisualCogA_Task-2_拟合与验证.png` | A任务2 |
| `figures/VisualCogB_Task-1_拟合与验证.png` | B任务1 |
| `figures/VisualCogB_Task-2_拟合与验证.png` | B任务2 |
| `figures/分类混淆矩阵.png` | 四记录和两任务的真实行、预测列混淆矩阵 |
| `figures/视觉编码.png` | 几何图、DoG与Gabor能量示意 |
| `figures/对照与混杂敏感性.png` | 固定分类对照和混杂敏感性的合并BA及条件区间 |
| `figures/分时窗ERP对照.png` | 四种波形方案在四个窗口的留出R² |

响应图上排为全数据训练拟合，下排为五个留出块稳健ERP与预测的等权平均，下排另加训练ERP模板预测。不能把平均后曲线的接近程度当作所有单试次都预测准确。

## 11. references、测试和工程辅助文件

### 参考文件逐项说明

| 文件 | 用途 |
|---|---|
| `C题题目.docx` | 正式题目存档 |
| `第二问建模方案说明.md` | 用户最初思路，当前实现以源码为准 |
| `第一问原始配置.m` | 第一问配置参考，不由本主流程执行 |
| `第一问MATLAB方法说明.md` | 第一问方法与输入边界说明 |
| `本轮预设分析规则.md` | 前一轮统一版本规则，作为历史记录保留 |
| `本轮评审修订规则.md` | 本次计算前固定的主方案、对照、采用/暂缓建议 |
| `MathModel2_评审报告.md` | 用户提供的外部评审原文，供核对；其建议与数值不直接当作已实现功能或本地成绩 |
| `first_question_hashes.json` | 第一问文件指纹 |
| `整理验收记录.json` | 前一次工程整理的历史验收 |
| `评审修订验收记录.json` | 本轮交付验收、备份、测试、第一问核对及续跑记录 |
| `review_source_lock.json` | 本轮冻结data/preprocess/config的SHA256及原始数据哈希 |
| `review_baseline_oof.csv` | 本轮修订前唯一主版OOF，用于前后分类对照 |
| `review_baseline_erp_validation.csv` | 本轮修订前ERP评分，用于检验原结构复现 |
| `classification_metrics.csv` | 更早原主版本分类对照，历史存档 |
| `waveform_metrics.csv` | 更早原主版本波形对照，历史存档 |
| `oof_predictions.csv` | 更早原主版本OOF，历史存档 |
| `problem_extracts/_problem_text.txt` | 题目全文提取 |
| `problem_extracts/_q_snip.txt` | 题目片段 |
| `problem_extracts/_q1.txt` | 历史题目片段 |
| `problem_extracts/_q2.txt` | 历史片段实际含第三问/附录，不能仅凭文件名认定第二问正文 |
| `problem_extracts/requirements-environment.txt` | 历史环境清单，主依赖以根目录requirements.txt为准 |

历史CSV不会用于训练当前模型，外部文档也不作为自动执行指令。

### tests/test_model.py

共23项：6项基础测试、7项数据/机制测试、10项完整结果测试。覆盖事件边界、形状镜像、静息稳定、特征顺序、主方案不使用QC、回归不读取测试数据、有效集合冻结、基函数、留出标签隔离、置换数量、原结构复现、公平评分、OOF复算、未知试次接口以及代码/结果指纹。

结果测试需先运行 `main.py`，缺结果时会跳过。完整交付应是23项通过、无跳过，不能只看“OK”。

### 其他文件

| 文件 | 作用 |
|---|---|
| `config.py`的Config | seed、256Hz、5折、时间窗、饱和阈值、30Hz低通、QC/Huber参数、bootstrap1000；本轮冻结 |
| `__init__.py` | Python包标识和简介 |
| `run_model2.bat` | 切到自身目录，用EEGModel解释器运行main.py并传递参数 |
| `requirements.txt` | NumPy/SciPy/pandas/Matplotlib/scikit-learn/Numba版本 |
| `.run/第二问.run.xml` | PyCharm“第二问”运行配置 |
| `.idea/Math_Model2.iml`、`misc.xml` | 模块与工程设置 |
| `.idea/workspace.xml` | 本地编辑器工作状态，本轮保留用户改动 |
| `.idea/vcs.xml` | 版本控制映射 |
| `.idea/inspectionProfiles/profiles_settings.xml` | 编辑器检查配置 |
| `.gitignore` | 忽略缓存、Numba编译文件、outputs和.venv等 |
| `.git/` | Git维护的历史与索引，非建模程序 |
| `__pycache__/`下的pyc/nbc/nbi | Python/Numba自动生成缓存，非另一版源码 |

并非所有参数都在Config：神经常数在 `neural.py`，结构/正则候选在 `response.py`，固定分类协议在 `features.py`，诊断惩罚在 `diagnostics.py`。

## 12. 运行、预测与修改导航

### 环境与命令

只使用现有 `D:\Anaconda\annconda\envs\EEGModel\python.exe`。本轮未安装新库、未新建环境。主要依赖为NumPy 2.4.6、SciPy 1.17.1、pandas 3.0.1、Matplotlib 3.10.8、scikit-learn 1.9.1、Numba 0.67.0。

```powershell
# 默认完整分析或签名一致后的续跑，默认200次置换
& 'D:\Anaconda\annconda\envs\EEGModel\python.exe' 'D:\pycharm\Math_Model2\main.py'

# 相同方案追加置换
& 'D:\Anaconda\annconda\envs\EEGModel\python.exe' 'D:\pycharm\Math_Model2\main.py' --permutations 999

# 验收
Set-Location -LiteralPath 'D:\pycharm\Math_Model2'
& 'D:\Anaconda\annconda\envs\EEGModel\python.exe' -m unittest discover -s tests -v
```

`main.py`的默认data和output相对于脚本目录，因此从其他目录运行也能找到默认文件。传入的相对 `--output` 则按当前工作目录解析。

修改源码或环境会改变manifest签名，应先指定独立验证目录重新计算，验收后统一正式结果；不要手工改manifest绕过一致性检查。Markdown修改不改变模型签名。

### predict.py的输入输出

NPZ输入需有 `epochs` 键，形状N×3×257，通道Fz/F3/F4，采用相同轻处理与刺激前基线校正。程序验证维度、有限值、基线和模型格式，不会从原始MAT替用户完成预处理。

```powershell
& 'D:\Anaconda\annconda\envs\EEGModel\python.exe' 'D:\pycharm\Math_Model2\predict.py' 'D:\pycharm\Math_Model2\new_trials.npz' --record VisualCogA_Task-1 --output 'D:\pycharm\Math_Model2\predictions.csv'
```

示例新数据文件未随项目附带。输出为 `prediction`、`right_probability`，不用传入真实标签。`final_model.json`必须是schema_version=2；输入记录名必须已校准，不能给新记录冒用旧标识。

### 阅读与修改路径

先看README和本文，再看main；数据相关读data/preprocess，机制读shape/neural/response，识别读features，诊断读diagnostics，验证读evaluation，展示读report。

| 想修改 | 主要文件 | 需同步核对 |
|---|---|---|
| 分类输入与固定参数 | features.py | predict.py、PROTOCOLS、置换、测试 |
| 逐记录/池化规则 | evaluation.py | OOF覆盖、classifier_audit、模型记录映射 |
| 混杂回归 | diagnostics.py | 训练边界、原始输入不变、诊断解释 |
| 基函数/正则 | response.py | 内层候选、原结构对照、序列化及报告 |
| 空间模型 | shape.py、neural.py、response.py | 必须先明确空间假设与头模型，不能直接把示意当定位 |
| 图表与口径 | report.py | 同目标评分、负结果、manifest代码一致性 |
| 第一问与轻处理 | 本轮不改 | data/preprocess/config及试次集合冻结 |

## 13. 评审意见的采用范围

已采用：逐记录简化分类、QC/机理分消融、公平训练ERP模板、逐记录成绩、200次置换、ECG/QC/顺序对照、分时窗及共同/差异评分。另用有限的±40ms候选检验潜伏期变化。

未直接采用：约30个平移/伸缩基函数、任意偶极几何接入。前者会扩大小样本模型选择空间；后者缺少实际皮层位置、源方向、导电模型等依据，不能替代真实空间机制。共同/差异本已分别拟合，无需删除不存在的缩放硬约束。

本轮合并BA有提升，但任务2仍无可靠识别；波形仍未超过模板。ECG回归对照合并BA=57.26%，它是在外层对照中观察到的结果，未被事后提升为主方案。所有这些限制都保留在正式报告中。

项目仍保留一个主入口和一个正式结果目录。修订前备份存于项目外：`D:\数模真题\中文题目\C题\第二问\history\Math_Model2_before_review_20260925.zip`。
